function preload() {
}

function setup() {
	createCanvas(600, 480);
}

function draw() {
	background(0);
}

function mouseClicked() {
	if (mouseX > 0 && mouseX < width && mouseY < height && mouseY > 0) { 
	}
}
